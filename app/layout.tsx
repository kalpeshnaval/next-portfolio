import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Kalpesh Naval Portfolio",
  description: "Hey, I’m Kalpesh Naval — a full-stack developer who enjoys turning ideas into fast, modern web apps using React and Next.js. I work with Tailwind CSS, Motion, Zod, and solid auth setups to build smooth, scalable experiences (and yes, I care about clean code).",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
